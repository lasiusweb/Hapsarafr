import React from 'react';

// This component is now obsolete due to the new OTP-based authentication flow.
// It is kept as an empty component to prevent breaking any potential lazy imports.
const AcceptInvitation: React.FC = () => {
    return null;
};

export default AcceptInvitation;